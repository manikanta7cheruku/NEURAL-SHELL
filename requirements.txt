# =============================================================================
# PROJECT SEVEN - Dependencies
# Version: 1.2.8
# =============================================================================

# --- Core ---
# CRITICAL: numpy MUST be pinned below 2.0. numpy 2.x breaks torch, faster-whisper,
# ctranslate2, chromadb, and sentence_transformers. Do not remove the version cap.
numpy>=1.26.0,<2.0.0

requests
colorama
setuptools
psutil

# --- Voice ---
pyttsx3
faster-whisper
SpeechRecognition
pyaudio

# --- App Control ---
AppOpener
pyautogui

# --- Memory System ---
chromadb==1.5.8
sentence-transformers

# --- Voice Recognition ---
# torch/torchaudio installed by bootstrap.py separately (CPU or GPU)
onnxruntime
# nemo_toolkit removed - Linux-only, Seven uses faster-whisper for STT

# --- Web Search ---
ddgs

# --- Window Mastery ---
pywin32

# --- System Control ---
pycaw
screen-brightness-control
comtypes

# --- API ---
fastapi
uvicorn[standard]
websockets
python-multipart

# --- Notifications ---
winotify

# --- Error tracking ---
sentry-sdk

# --- Proactive Features ---
pynput

# --- Fuzzy Matching ---
rapidfuzz

# --- Acoustic Echo Cancellation ---
# pyaudiowpatch provides WASAPI loopback for AEC
# Falls back gracefully if not installed
pyaudiowpatch

# --- Torch (GPU support) ---
# Install separately with CUDA:
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/nightly/cu128
# torch and torchaudio NOT in this file - GPU version installed manually

# --- Torch ---
# CPU only (auto-installed during setup):
# torch torchaudio
# GPU (CUDA 12.8, RTX 50 series / Blackwell):
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/nightly/cu128
# GPU (CUDA 12.1, RTX 30/40 series):
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121